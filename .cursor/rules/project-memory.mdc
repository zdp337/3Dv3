---
description: 项目记忆维护约定：开发记录与跨设备上下文同步
alwaysApply: true
---

# 项目记忆维护

- 项目背景、工程约定见根目录 `AGENTS.md`；进行中的大事项方案见 `docs/` 下的方案文档。
- **每完成一项会改动代码的任务，必须在 `docs/DEVLOG.md` 顶部追加条目**，格式：日期 / 改动内容 / 原因 / 验收方式。重要的技术决策（即使不改代码）也要记录结论。
- 聊天记录不随 git 同步。任何需要跨设备/跨会话延续的上下文（方案、待办、踩坑结论），都要落到上述文件里，不要只留在对话中。
- 验收以 `miniprogram/` 小程序工程为准；根目录静态原型已停止维护。
