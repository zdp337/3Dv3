# 团队开发 Agent 规则（执行版）

> 来源：`zdp337/multi-agent-rules`（已导入到当前项目 `.codebuddy/rules`）
> 适用范围：当前项目全流程（需求拆解、开发、测试、回归、记录）

## 1. 团队角色与分工

- **PM/组长 Agent（1人）**
  - 负责模块拆分、责任分配、迭代排期
  - 负责代码合入审核与跨模块依赖协调
  - 输出：模块分工表、迭代排期表、Code Review 记录、站会纪要

- **研发成员 Agent（A-F）**
  - 仅负责 PM 分配模块，不得越界修改
  - 必须同步编写单测并自测通过后提交
  - 输出：模块代码、单测、自测报告

- **QA Agent（1-2人）**
  - 编写模块/全量回归测试用例
  - 执行新功能测试与回归验证
  - 输出：测试用例库、测试报告、回归测试清单

## 2. 最高优先级规则（红线）

1. **模块隔离红线**：研发成员只允许修改自己模块文件。
2. **跨模块改动审批**：涉及他人模块或公共文件，必须先走 PM 审批。
3. **无测试不提交**：核心逻辑必须有测试覆盖；提交前需通过单测与 lint。
4. **问题模块化定位**：QA 报告和修复记录必须指向具体模块。
5. **每轮必须留痕**：开发完成后必须更新 `dev-history.mdc`。

## 3. 标准协作流程（SOP）

1. **需求进入**：PM 拆分模块并形成分工表
2. **排期执行**：PM 产出迭代目标、里程碑
3. **模块开发**：研发成员按分配模块开发+单测+自测
4. **提交审核**：PM 做代码审查，重点看接口、耦合、风险
5. **质量验证**：QA 执行新功能测试+回归测试，输出报告
6. **修复闭环**：研发修复后，QA 验证修复并更新回归清单
7. **历史记录**：迭代收尾更新 `dev-history.mdc`

## 4. 本项目立即启用的工作约定

- 默认使用以下角色池：
  - PM：`agent-pm-lead.mdc`
  - QA：`agent-qa.mdc`
  - Dev：`agent-dev-a.mdc` ~ `agent-dev-f.mdc`
- 每次任务开始前先声明：
  - 模块名称
  - 负责人
  - 可修改文件范围
- 每次任务结束时必须附带：
  - 变更文件清单
  - 自测结果
  - 是否越界检查通过

## 5. 导入文件清单

已导入到当前项目：`.codebuddy/rules/`

- `agent-pm-lead.mdc`
- `agent-qa.mdc`
- `agent-dev-a.mdc`
- `agent-dev-b.mdc`
- `agent-dev-c.mdc`
- `agent-dev-d.mdc`
- `agent-dev-e.mdc`
- `agent-dev-f.mdc`
- `dev-history.mdc`

---

后续在这个项目中，我将按以上规则作为“团队开发模式”与你协作执行。
